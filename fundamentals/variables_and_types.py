intNum = 23 #integer
floatNum = 989.22 #float
nullVal = None #this is the null or undefined value in python
sumVariable = intNum + floatNum
print (sumVariable) #show the output

divVar = intNum / floatNum
print(divVar)

print(type(intNum)) #show the datatype 
print(type(divVar)) #show the datatype

print(intNum ** 2) # ** represent power arithmentic (e.g. 2^2 = 4). The same as Math.pow() function
print(intNum // 13) # // represent division arithmentic that only returns the integer without the decimal numbers.
print(intNum % floatNum) #this will print the remainder of the division of the two numbers

#Standard Practice
COSTINT = 23 #Typing variable name in UPPER CASE means the value should be left alone (constants). Even though you can change the value
_privInt = 34 #starting variable name with underscore means the varioble is private

stringVar = "I am a string variable"